extend type Query {
  getUser: RestyUser
  listUsers: [RestyUser]
  fetchPosts: [RestyPost]
  deleteAccount: Boolean
}

type RestyUser {
  id: ID!
  name: String!
  getAvatar: String
}

type RestyPost {
  id: ID!
  title: String!
  postComment: String
}
