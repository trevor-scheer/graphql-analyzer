# Toolchain pins consumed by mise (https://mise.jdx.dev) and the
# jdx/mise-action GitHub Action. Node version comes from `.node-version`
# (also honored by mise) so it stays a universal pin.
[tools]
pnpm = "10"
